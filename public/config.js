export default({
  tftNetwork: 'testnet',
  appId: `${window.location.host}`,
  botFrontEnd: 'http://localhost:8082',
  botBackend: 'http://localhost:5000',
  redirect_url: '/login',
  scope: 'user:keys'
})
